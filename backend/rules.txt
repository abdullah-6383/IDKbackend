---
alwaysApply: true
Rule1: never create summary md files if it will not help you to regain context
rule2: always put the md files you make inside md_files folder
rule3: do not ever create md files for user reference
rule4: always follow code structure
rule5: never do patch works, always analyse the algorithm and rebuild it never edit and add logs to simply patch it 
rule6: make every part of code as optimised as possible , most important
rule7: do not add unnecessay logs and code blocks that can slow down performance unless u need to debug.
rule8: NEVER FUCKING USE EMOJIES or the double "hypen" IN PRODUCTION code and contents in and out of application
rule9: For every change u make, review to make sure older logic doesnt break, make sure u mitigate any errors and MIGRATE the logic properly.
---